# Sample Environment Variables

# The base URL to the upstream service
UPSTREAM_BASE_URL=http://example.com

# The data directory
DATA_DIR=/path/to/data

# Admin token for /admin/* endpoints (required for security)
ADMIN_TOKEN=your-secure-admin-token-here

# Server port (default: 8866)
PORT=8866

# Event logging configuration
# Maximum number of SSE chunks to store per event (default: 50)
MAX_LOG_CHUNKS=50

# Maximum size of prompt/response in event logs in characters (default: 2000)
MAX_LOG_FIELD_SIZE=2000

# Enable full trace logging (stores complete prompts/responses, default: false)
FULL_TRACE_LOGGING=false
